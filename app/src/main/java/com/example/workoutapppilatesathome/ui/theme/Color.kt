package com.example.workoutapppilatesathome.ui.theme

import androidx.compose.ui.graphics.Color

// Ruhige, an Pilates/Wellness angelehnte Farbpalette. Erfüllt die Design-Vorgabe
// "weiß, grün mit pinken Akzenten" aus der Aufgabenstellung, aber mit gedämpfteren,
// weniger grellen Tönen als reines Signalgrün/Knallpink.

val PrimaryGreen = Color(0xFF5B8C6E)      // gedämpftes Salbeigrün - Primärfarbe (z.B. Buttons)
val PrimaryGreenDark = Color(0xFF3E6650)  // dunklerer Grünton für Statusleiste & App-Icon-Hintergrund
val AccentPink = Color(0xFFE2897E)        // sanftes Terracotta/Koralle als "pinker" Akzent (Vorgabe)
val BackgroundWhite = Color(0xFFFBF9F5)   // warmes Off-White statt reinem Weiß (Vorgabe: weiß)
val SurfaceCard = Color(0xFFFFFFFF)       // reines Weiß für Karten, damit sie sich vom Hintergrund abheben
val LightGray = Color(0xFFEDEAE3)         // dezenter Grauton, z.B. für Trenner/Platzhalter
val TextWhite = Color(0xFFFFFFFF)         // Textfarbe auf farbigem Untergrund (z.B. Button-Beschriftung)
val TextBlack = Color(0xFF2B2B28)         // Haupttextfarbe (kein reines Schwarz, etwas weicher)
val TextMuted = Color(0xFF6F6F68)         // gedämpfte Textfarbe für Untertitel/sekundäre Infos
